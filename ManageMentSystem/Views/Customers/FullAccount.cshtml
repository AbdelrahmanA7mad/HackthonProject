@model ManageMentSystem.ViewModels.CustomerFullAccountViewModel
@{
    ViewData["Title"] = "حساب العميل بالكامل";
}

<div class="min-h-screen bg-bgSubtle p-6">

    <div class="flex flex-col md:flex-row justify-between items-center gap-4 mb-8">
        <div>
            <h1 class="text-2xl font-bold text-gray-800 flex items-center gap-2">
                <i data-lucide="receipt" class="text-primary w-6 h-6"></i>
                حساب العميل بالكامل
            </h1>
            <div class="flex items-center gap-3 mt-2 text-md font-bold text-gray-500 mt-2">
                <span class="flex items-center gap-1 bg-gray-200 text-gray-800 px-3 py-1 rounded-lg">
                    <i data-lucide="user" class="text-primary w-4 h-4"></i> @Model.CustomerName
                </span>
                <span class="flex items-center gap-1 bg-gray-200 text-gray-800 px-3 py-1 rounded-lg">
                    <i data-lucide="phone" class="text-accentGreen w-4 h-4"></i> @(Model.CustomerPhone ?? "-")
                </span>
            </div>
        </div>

        <a href="@Url.Action("Statement", "Customers", new { id = Model.CustomerId })" class="zen-btn zen-btn-neutral">
            رجوع للكشف المختصر<i data-lucide="arrow-left" class="w-4 h-4"></i>
        </a>
    </div>

    <div class="bg-white rounded-xl shadow-md border border-gray-200 overflow-hidden">
        <div class="bg-gray-50 px-6 py-4 border-b border-gray-200 flex items-center justify-between">
            <h6 class="font-bold text-gray-800 flex items-center gap-2">
                <i data-lucide="list" class="text-primary w-5 h-5"></i> سجل العمليات التفصيلي
            </h6>
            <div class="text-xs font-bold text-gray-500">
                إجمالي العمليات: @Model.TotalItems
            </div>
        </div>

        <div class="overflow-x-auto">
            <table class="w-full text-right">
                <thead>
        <tr class="bg-bgSubtle text-gray-800 border-b border-gray-200 text-sm text-center">
                        <th class="px-6 py-4 font-bold">التاريخ</th>
                        <th class="px-6 py-4 font-bold">النوع</th>
                        <th class="px-6 py-4 font-bold">المبلغ</th>
                        <th class="px-6 py-4 font-bold">طريقة الدفع</th>
                        <th class="px-6 py-4 font-bold">تراكمي فواتير</th>
                        <th class="px-6 py-4 font-bold">تراكمي مدفوع</th>
                        <th class="px-6 py-4 font-bold">الرصيد التراكمي</th>
                        <th class="px-6 py-4 font-bold">التفاصيل</th>
                        <th class="px-6 py-4 font-bold text-center">إجراءات</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-100 text-center font-bold">
                    @foreach (var e in Model.Entries)
                    {
                        // تحديد لون الصف بناءً على نوع العملية
                        var rowClass = "";
                        var typeBadgeClass = "";
                        var typeIcon = "";

                        if (e.Type == "مرتجع")
                        {
                            rowClass = "bg-gray-50/30 hover:bg-red-50";
                            typeBadgeClass = "bg-red-100 text-red-800 border-red-200";
                            typeIcon = "rotate-ccw";
                        }
                        else if (e.Type == "دفعة")
                        {
                            rowClass = "bg-gray-50/30 hover:bg-gray-50";
                            typeBadgeClass = "bg-emerald-100 text-accentGreen border-gray-200";
                            typeIcon = "hand-coins";
                        }
                        else // فاتورة
                        {
                            rowClass = "hover:bg-gray-50";
                            typeBadgeClass = "bg-gray-100 text-primary border-gray-200";
                            typeIcon = "file-text";
                        }

                        <tr class="transition-colors @rowClass group">
                            <td class="px-6 py-4 text-sm font-bold text-gray-700 font-mono">
                                @e.Date.ToString("yyyy-MM-dd")
                            </td>
                            <td class="px-6 py-4">
                                <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-lg text-sm font-bold border @typeBadgeClass">
                                    <i data-lucide="@typeIcon" class="w-4 h-4"></i> @e.Type
                                </span>
                            </td>
                            <td class="px-6 py-4 text-sm font-bold text-gray-800">
                                @e.Amount.ToString("C")
                            </td>
                            <td class="px-6 py-4">
                                @if (!string.IsNullOrEmpty(e.PaymentMethodName))
                                {
                                    <span class="inline-block px-2 py-1 bg-gray-100 text-gray-600 rounded text-sm font-bold border border-gray-200">
                                        @e.PaymentMethodName
                                    </span>
                                }
                                else if (!string.IsNullOrEmpty(e.SalePaymentTypeName))
                                {
                                    <span class="inline-block px-2 py-1 bg-gray-50 text-primary rounded text-sm font-bold border border-gray-200">
                                        @e.SalePaymentTypeName
                                    </span>
                                }
                                else
                                {
                                    <span class="text-gray-500">-</span>
                                }
                            </td>
                            <td class="px-6 py-4 text-sm font-bold text-primary">
                                @e.RunningSales.ToString("C")
                            </td>
                            <td class="px-6 py-4 text-sm font-bold text-accentGreen">
                                @e.RunningPaid.ToString("C")
                            </td>
                            <td class="px-6 py-4 text-sm font-bold @(e.RunningBalance > 0 ? "text-accentRed" : "text-accentGreen")">
                                @e.RunningBalance.ToString("C")
                            </td>
                            <td class="px-6 py-4">
                                @if (e.SaleId.HasValue)
                                {
                                    <a href="@Url.Action("Details", "Sales", new { id = e.SaleId.Value })" class="text-primary hover:text-primary font-bold text-sm underline decoration-indigo-200 hover:decoration-indigo-900 transition-all">
                                        عرض الفاتورة <i data-lucide="external-link" class="ml-1 w-3 h-3 inline-block"></i>
                                    </a>
                                }
                                else if (e.PaymentId.HasValue)
                                {
                                    <div class="flex flex-col gap-1">
                                        @if (!string.IsNullOrEmpty(e.Notes))
                                        {
                                            <span class="text-sm text-gray-500 font-medium">@e.Notes</span>
                                        }

                                        @if (e.Allocations != null && e.Allocations.Any())
                                        {
                                            <div class="text-[10px] text-gray-500 font-bold mt-1">
                                                موزعة على:
                                                <div class="flex flex-wrap gap-1 mt-0.5">
                                                    @foreach (var a in e.Allocations)
                                                    {
                                                        <span class="bg-gray-50 px-1 rounded border border-gray-200">
                                                            #@a.SaleId: @a.Amount.ToString("C0")
                                                        </span>
                                                    }
                                                </div>
                                            </div>
                                        }
                                    </div>
                                }
                                else if (e.ReturnId.HasValue)
                                {
                                    <a href="@Url.Action("Details", "Returns", new { id = e.ReturnId.Value })" class="text-amber-600 hover:text-amber-800 font-bold text-sm underline decoration-amber-200 hover:decoration-amber-800 transition-all">
                                        عرض المرتجع <i data-lucide="external-link" class="ml-1 w-3 h-3 inline-block"></i>
                                    </a>
                                }
                            </td>
                            <td class="px-6 py-4 text-center">
                                <div class="">
                                    @if (e.SaleId.HasValue)
                                    {
 <button type="button" onclick="deleteSaleEntry(@e.SaleId.Value)" class="zen-action-btn zen-action-btn-danger" title="حذف الفاتورة">
                                            <i data-lucide="trash-2" class="w-4 h-4"></i>
                                        </button>
                                    }
                                    else if (e.PaymentId.HasValue)
                                    {
 <button type="button" onclick="deletePaymentEntry(@e.PaymentId.Value)" class="zen-action-btn zen-action-btn-danger" title="حذف الدفعة">
                                            <i data-lucide="trash-2" class="w-4 h-4"></i>
                                        </button>
                                    }
                                    else if (e.ReturnId.HasValue)
                                    {
 <button type="button" onclick="deleteReturnEntry(@e.ReturnId.Value)" class="zen-action-btn zen-action-btn-danger" title="حذف المرتجع">
                                            <i data-lucide="trash-2" class="w-4 h-4"></i>
                                        </button>
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        @if (Model.TotalPages > 1)
        {
            <div class="bg-gray-50 px-6 py-4 border-t border-gray-200 flex flex-col sm:flex-row justify-between items-center gap-4">
                <div class="text-sm font-bold text-gray-500">
                    عرض @(((Model.CurrentPage - 1) * Model.PageSize) + 1) إلى @Math.Min(Model.CurrentPage * Model.PageSize, Model.TotalItems) من @Model.TotalItems عملية
                </div>

                <div class="flex gap-1">
                    @if (Model.CurrentPage > 1)
                    {
                        <a href="@Url.Action("FullAccount", "Customers", new { id = Model.CustomerId, page = Model.CurrentPage - 1, pageSize = Model.PageSize })"
                           class="w-9 h-9 flex items-center justify-center rounded-lg border border-gray-200 bg-white text-gray-600 hover:bg-gray-50 hover:text-primary transition-all">
                            <i data-lucide="chevron-right" class="w-4 h-4"></i>
                        </a>
                    }

                    @for (int i = Math.Max(1, Model.CurrentPage - 2); i <= Math.Min(Model.TotalPages, Model.CurrentPage + 2); i++)
                    {
                        <a href="@Url.Action("FullAccount", "Customers", new { id = Model.CustomerId, page = i, pageSize = Model.PageSize })"
                           class="w-9 h-9 flex items-center justify-center rounded-lg border text-sm font-bold transition-all @(i == Model.CurrentPage ? "bg-primary text-white border-gray-200 shadow-sm" : "bg-white border-gray-200 text-gray-600 hover:bg-gray-50 hover:text-primary")">
                            @i
                        </a>
                    }

                    @if (Model.CurrentPage < Model.TotalPages)
                    {
                        <a href="@Url.Action("FullAccount", "Customers", new { id = Model.CustomerId, page = Model.CurrentPage + 1, pageSize = Model.PageSize })"
                           class="w-9 h-9 flex items-center justify-center rounded-lg border border-gray-200 bg-white text-gray-600 hover:bg-gray-50 hover:text-primary transition-all">
                            <i data-lucide="chevron-left" class="w-4 h-4"></i>
                        </a>
                    }
                </div>
            </div>
        }
    </div>
</div>

@Html.AntiForgeryToken()

@section Scripts {
    <script src="~/js/sweetalert-customers.js"></script>
    <script>
        async function postWithToken(url, data) {
            const tokenInput = document.querySelector('input[name="__RequestVerificationToken"]');
            const headers = { 'X-Requested-With': 'XMLHttpRequest' };
            let body;

            // استخدام FormData لإرسال التوكن والبيانات بشكل صحيح
            const formData = new FormData();
            if (tokenInput) {
                formData.append('__RequestVerificationToken', tokenInput.value);
            }

            for (const k in data) {
                formData.append(k, data[k]);
            }

            // عند استخدام FormData، لا نحدد Content-Type يدوياً
            const res = await fetch(url, {
                method: 'POST',
                headers: headers,
                body: formData
            });

            try { return await res.json(); } catch { return { success: res.ok }; }
        }

        function deleteEntry(type, id, url) {
            Swal.fire({
                title: 'تأكيد الحذف',
                text: `هل أنت متأكد من حذف هذا ${type}؟`,
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: accentRed,
                cancelButtonColor: textMuted,
                confirmButtonText: 'نعم، احذف',
                cancelButtonText: 'إلغاء'
            }).then(async (result) => {
                if (result.isConfirmed) {
                    const res = await postWithToken(url, { id });
                    if (res.success) {
                        Swal.fire({
                            icon: 'success',
                            title: 'تم الحذف',
                            text: 'تم حذف العملية بنجاح',
                            timer: 1500,
                            showConfirmButton: false
                        }).then(() => location.reload());
                    } else {
                        Swal.fire('خطأ', res.message || 'فشل الحذف', 'error');
                    }
                }
            });
        }

        function deleteSaleEntry(id) {
            deleteEntry('الفاتورة', id, '@Url.Action("Delete", "Sales")');
        }

        function deletePaymentEntry(id) {
            deleteEntry('الدفعة', id, '@Url.Action("DeletePayment", "Customers")');
        }

        function deleteReturnEntry(id) {
            deleteEntry('المرتجع', id, '@Url.Action("Delete", "Returns")');
        }
    </script>
}
