@model ManageMentSystem.Models.PaymentMethodOption

@{
    ViewData["Title"] = "حذف طريقة دفع";
}

<div class="container mx-auto px-4 py-6 max-w-2xl">
    <div class="bg-white rounded-xl shadow-lg overflow-hidden">
        <!-- Header -->
        <div class=" from-red-600 to-red-700 px-6 py-4">
            <h3 class="text-2xl font-bold text-white flex items-center gap-3">
                <i data-lucide="trash-2" class="w-6 h-6"></i>
                حذف طريقة دفع
            </h3>
        </div>

        <!-- Warning Alert -->
        <div class="bg-red-50 border-r-4 border-red-500 text-red-800 p-4 m-6 rounded-lg flex items-center gap-3">
            <i data-lucide="alert-triangle" class="text-red-500 w-8 h-8"></i>
            <span class="font-semibold">هل أنت متأكد من حذف هذه الطريقة؟</span>
        </div>

        <!-- Details Card -->
        <div class="mx-6 mb-6 bg-gray-50 rounded-lg p-6 border border-gray-200">
            <dl class="space-y-4">
                <div class="flex items-center justify-between border-b border-gray-200 pb-3">
                    <dt class="text-sm font-semibold text-gray-600 flex items-center gap-2">
                        <i data-lucide="tag" class="text-primary w-4 h-4"></i>
                        الاسم
                    </dt>
                    <dd class="text-base font-bold text-gray-900">@Model.Name</dd>
                </div>
                <div class="flex items-center justify-between border-b border-gray-200 pb-3">
                    <dt class="text-sm font-semibold text-gray-600 flex items-center gap-2">
                        <i data-lucide="star" class="text-amber-600 w-4 h-4"></i>
                        افتراضي
                    </dt>
                    <dd class="text-base font-medium text-gray-900">
                        @if (Model.IsDefault)
                        {
                            <span class="bg-amber-100 text-amber-800 px-3 py-1 rounded-full text-sm">نعم</span>
                        }
                        else
                        {
                            <span class="text-gray-500">لا</span>
                        }
                    </dd>
                </div>
                <div class="flex items-center justify-between">
                    <dt class="text-sm font-semibold text-gray-600 flex items-center gap-2">
                        <i data-lucide="toggle-right" class="text-green-600 w-4 h-4"></i>
                        نشط
                    </dt>
                    <dd class="text-base font-medium text-gray-900">
                        @if (Model.IsActive)
                        {
                            <span class="bg-green-100 text-green-800 px-3 py-1 rounded-full text-sm">نعم</span>
                        }
                        else
                        {
                            <span class="bg-gray-100 text-gray-600 px-3 py-1 rounded-full text-sm">لا</span>
                        }
                    </dd>
                </div>
            </dl>
        </div>

        <!-- Actions -->
        <form asp-action="Delete" method="post" class="p-6 pt-0">
            <input type="hidden" asp-for="Id" />
            <div class="flex gap-3">
                <button type="submit" class="zen-btn zen-btn-danger flex-1">
                    <i data-lucide="trash" class="w-5 h-5"></i>
                    تأكيد الحذف
                </button>
                <a asp-action="Index" class="zen-btn zen-btn-neutral flex-1">
                    إلغاء
                    <i data-lucide="arrow-left" class="w-5 h-5"></i>
                </a>
            </div>
        </form>
    </div>
</div>
