@model IEnumerable<ManageMentSystem.Models.Sale>
@using ManageMentSystem.Helpers
@using ManageMentSystem.Models
@{
    ViewData["Title"] = "المبيعات";
}

<div class="min-h-screen bg-bgSubtle p-6">

    <div class="flex flex-col md:flex-row justify-between items-center gap-4 mb-8">
        <div>
            <h1 class="text-2xl font-bold text-primary flex items-center gap-2">
                <i data-lucide="shopping-cart" class="text-primary"></i> إدارة المبيعات
            </h1>
            <div class="flex items-center gap-2 mt-4">
                <span class="text-sm font-bold text-gray-600">المستخدم الحالي:</span>
                <span class="bg-gray-50 text-primary px-3 py-1 rounded-lg text-sm font-bold border border-gray-200 flex items-center gap-1">
                    <i data-lucide="user-circle" class="w-4 h-4"></i> @ViewBag.CurrentUser?.FullName
                </span>
            </div>
        </div>

        <div class="flex gap-3">
            <a asp-action="UnpaidSales" class="zen-btn zen-btn-neutral zen-btn-sm">
                <i data-lucide="clock" class="w-4 h-4 me-1"></i> مبيعات غير مدفوعة
            </a>
                <a asp-action="QuickSale" class="zen-btn zen-btn-primary zen-btn-sm">
                    <i data-lucide="banknote" class="w-4 h-4 text-white me-1"></i> شاشة البيع
                </a>
        </div>
    </div>

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="mb-6 bg-red-50 border border-red-200 text-red-800 px-4 py-3 rounded-xl flex items-center gap-3 shadow-sm relative" role="alert">
            <i data-lucide="alert-triangle" class="text-xl"></i>
            <span class="font-bold">@TempData["ErrorMessage"]</span>
            <button type="button" class="absolute left-4 top-3 text-red-400 hover:text-red-600" data-bs-dismiss="alert"><i data-lucide="x" class="w-4 h-4"></i></button>
        </div>
    }

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="mb-6 bg-emerald-50 border border-emerald-200 text-emerald-800 px-4 py-3 rounded-xl flex items-center gap-3 shadow-sm relative" role="alert">
            <i data-lucide="check-circle" class="text-xl"></i>
            <span class="font-bold">@TempData["SuccessMessage"]</span>
            <button type="button" class="absolute left-4 top-3 text-emerald-400 hover:text-emerald-600" data-bs-dismiss="alert"><i data-lucide="x" class="w-4 h-4"></i></button>
        </div>
    }

    <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6 mb-8">
        <form method="get" action="@Url.Action("Index", "Sales")" class="grid grid-cols-1 md:grid-cols-4 gap-4 items-end">
            <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">البحث العام</label>
                <div class="relative">
                    <input type="text" name="searchTerm" value="@ViewBag.SearchTerm"
                           class="w-full pl-4 pr-10 py-2.5 border border-gray-200 rounded-lg focus:ring-1 focus:ring-primary focus:border-primary outline-none text-sm font-bold transition-all"
                           placeholder="اسم العميل أو رقم الفاتورة...">
                    <i data-lucide="search" class="absolute left-3 top-3 text-gray-500 w-4 h-4"></i>
                </div>
            </div>

            <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">نوع الفاتورة</label>
                <select name="paymentType" class="w-full px-4 py-2.5 border border-gray-200 rounded-lg focus:ring-1 focus:ring-primary focus:border-primary outline-none text-sm font-bold transition-all bg-white">
                    <option value="">الكل</option>
                    <option value="1" selected="@(ViewBag.PaymentType == ManageMentSystem.Models.SalePaymentType.Cash)">كاش</option>
                    <option value="2" selected="@(ViewBag.PaymentType == ManageMentSystem.Models.SalePaymentType.Partial)">جزئي</option>
                    <option value="3" selected="@(ViewBag.PaymentType == ManageMentSystem.Models.SalePaymentType.Credit)">آجل</option>
                </select>
            </div>

            <div>
                <label class="block text-sm font-bold text-gray-700 mb-2">تاريخ الفاتورة</label>
                <input type="date" name="date" value="@ViewBag.date" class="w-full px-4 py-2.5 border border-gray-200 rounded-lg focus:ring-1 focus:ring-primary focus:border-primary outline-none text-sm font-bold transition-all text-gray-600">
            </div>

            <div class="flex gap-2">
                <button type="submit" class="zen-btn zen-btn-primary zen-btn-sm zen-btn-block flex-1">
                    <i data-lucide="filter" class="w-4 h-4 inline-block ml-1"></i> تصفية
                </button>
                <a href="@Url.Action("Index", "Sales")" class="zen-btn zen-btn-neutral zen-btn-sm">
                    <i data-lucide="x" class="w-4 h-4"></i>
                </a>
            </div>
        </form>
    </div>

    <div class="bg-white rounded-xl shadow-md border border-gray-200 overflow-hidden">
        <div class="bg-gray-50 px-6 py-4 border-b border-gray-200 flex justify-between items-center">
            <h6 class="font-bold text-gray-800">سجل المبيعات</h6>
            <span class="text-xs font-bold text-gray-700 bg-white px-2 py-1 rounded border border-gray-200">العدد: @Model.Count()</span>
        </div>

        @if (Model.Any())
        {
            <div class="overflow-x-auto">
                <table class="w-full text-right" id="salesTable">
                    <thead>
        <tr class="bg-bgSubtle text-primary border-b border-gray-200 text-sm text-center">
                            <th class="px-6 py-4 font-bold"># الفاتورة</th>
                            <th class="px-6 py-4 font-bold">العميل</th>
                            <th class="px-6 py-4 font-bold">التاريخ</th>
                            <th class="px-6 py-4 font-bold">المبلغ</th>
                            <th class="px-6 py-4 font-bold">النوع</th>
                            <th class="px-6 py-4 font-bold">الحالة</th>
                            <th class="px-6 py-4 font-bold text-center">الإجراءات</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-100 text-center font-bold text-sm">
                        @foreach (var item in Model)
                        {
                            <tr class="hover:bg-gray-50 transition-colors group">
                                <td class="px-6 py-4  font-mono ">
                                    <div class="flex items-center gap-1.5 text-primary bg-gray-50 px-2.5 py-1 font-bold rounded-full w-fit border border-gray-200">

                                        <span class="font-bold"># @item.Id</span>
                                    </div>
                                </td>


                                <td class="px-6 py-4">
                                    <a href="@Url.Action("Statement", "Customers", new { id = item.CustomerId })" class="flex items-center gap-2 text-gray-700 hover:text-primary transition-colors">
                                        <div class="w-8 h-8 rounded-full bg-gray-100 flex items-center justify-center text-gray-600 ">
                                            <i data-lucide="user" class="w-4 h-4"></i>
                                        </div>
                                        @item.Customer?.FullName
                                    </a>
                                </td>
                                <td class="px-6 py-4 text-gray-600 text-sm">@item.SaleDate.ToString("dd/MM/yyyy")</td>
                                <td class="px-6 py-4">
                                    <span class="text-accentGreen font-bold bg-emerald-50 px-2 py-1 rounded-full border border-emerald-100">
                                        @item.TotalAmount.ToString("C")
                                    </span>
                                </td>
                                <td class="px-6 py-4">
                                    <span class=" font-bold px-2 py-1 rounded-full border bg-gray-50 text-gray-700 border-gray-200">
                                        @item.PaymentType
                                    </span>
                                </td>
                                <td class="px-6 py-4">
                                    @{
                                        var isFullyPaid = item.RemainingAmount <= 0;
                                        var isPartial = item.PaidAmount > 0 && item.RemainingAmount > 0;
                                    }
                                    @if (isFullyPaid)
                                    {
                                        <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-full  bg-emerald-50 text-accentGreen border border-emerald-200">
                                            <i data-lucide="check" class="text-[10px] w-3 h-3"></i> خالص
                                        </span>
                                    }
                                    else if (isPartial)
                                    {
                                        <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-full bg-amber-50 text-amber-700 border border-amber-200">
                                            <i data-lucide="clock" class="text-[10px] w-3 h-3"></i> جزئي
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="inline-flex items-center gap-1 px-2.5 py-1 rounded-full bg-red-50 text-accentRed border border-red-200">
                                            <i data-lucide="x" class="text-[10px] w-3 h-3"></i> غير مسدد
                                        </span>
                                    }
                                </td>
                                <td class="px-6 py-4">
                                    <div class="flex items-center justify-center gap-2 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity">
                                        <button type="button" onclick="viewSale(@item.Id)" class="zen-action-btn zen-action-btn-info" title="عرض">
                                            <i data-lucide="eye" class="w-4 h-4"></i>
                                        </button>

                                            <a href="@Url.Action("ReceiptInvoice", new { id = item.Id })" target="_blank" class="zen-action-btn zen-action-btn-neutral" title="طباعة">
                                                <i data-lucide="printer" class="w-4 h-4"></i>
                                            </a>

                                            <a href="#" onclick="sendInvoiceWhatsapp(@item.Id, this); return false;" class="zen-action-btn zen-action-btn-info" title="واتساب">
                                                <i data-lucide="message-circle" class="w-4 h-4"></i>
                                                <span class="spinner-border spinner-border-sm hidden" role="status"></span>
                                            </a>

                                            <a href="@Url.Action("ReturnFromSale", "Sales", new { id = item.Id })" class="w-8 h-8 flex items-center justify-center rounded-lg text-amber-700 hover:text-amber-700 hover:bg-amber-100 transition-colors" title="استرجاع">
                                                <i data-lucide="undo-2" class="w-4 h-4"></i>
                                            </a>

                                            <a asp-action="Edit" asp-route-id="@item.Id" class="zen-action-btn zen-action-btn-neutral" title="تعديل">
                                                <i data-lucide="edit" class="w-4 h-4"></i>
                                            </a>
                                            <button type="button" onclick="deleteSale(@item.Id)" class="zen-action-btn zen-action-btn-danger" title="حذف">
                                                <i data-lucide="trash-2" class="w-4 h-4"></i>
                                            </button>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            @if (ViewBag.TotalPages > 1)
            {
                <div class="bg-gray-50 px-6 py-4 border-t border-gray-200 flex flex-col sm:flex-row justify-between items-center gap-4">
                    <div class="text-sm font-bold text-gray-500">
                        عرض @(((ViewBag.CurrentPage - 1) * ViewBag.PageSize) + 1) إلى @Math.Min(ViewBag.CurrentPage * ViewBag.PageSize, ViewBag.TotalCount) من @ViewBag.TotalCount مبيعات
                    </div>

                    <div class="flex gap-1">
                        @if (ViewBag.CurrentPage > 1)
                        {
                            <a href="@Url.Action("Index", "Sales", new { page = ViewBag.CurrentPage - 1, searchTerm = ViewBag.SearchTerm, startDate = ViewBag.StartDate, endDate = ViewBag.EndDate })"
                               class="w-9 h-9 flex items-center justify-center rounded-lg border border-gray-200 bg-white text-gray-600 hover:bg-gray-50 hover:text-primary transition-all">
                                <i data-lucide="chevron-right" class="w-4 h-4"></i>
                            </a>
                        }

                        @for (int i = Math.Max(1, ViewBag.CurrentPage - 2); i <= Math.Min(ViewBag.TotalPages, ViewBag.CurrentPage + 2); i++)
                        {
                            <a href="@Url.Action("Index", "Sales", new { page = i, searchTerm = ViewBag.SearchTerm, startDate = ViewBag.StartDate, endDate = ViewBag.EndDate })"
                               class="w-9 h-9 flex items-center justify-center rounded-lg border text-sm font-bold transition-all @(i == ViewBag.CurrentPage ? "bg-primary text-white border-primary shadow-sm" : "bg-white border-gray-200 text-gray-600 hover:bg-gray-50 hover:text-primary")">
                                @i
                            </a>
                        }

                        @if (ViewBag.CurrentPage < ViewBag.TotalPages)
                        {
                            <a href="@Url.Action("Index", "Sales", new { page = ViewBag.CurrentPage + 1, searchTerm = ViewBag.SearchTerm, startDate = ViewBag.StartDate, endDate = ViewBag.EndDate })"
                               class="w-9 h-9 flex items-center justify-center rounded-lg border border-gray-200 bg-white text-gray-600 hover:bg-gray-50 hover:text-primary transition-all">
                                <i data-lucide="chevron-left" class="w-4 h-4"></i>
                            </a>
                        }
                    </div>
                </div>
            }
        }
        else
        {
            <div class="text-center py-12">
                <div class="bg-gray-50 w-20 h-20 rounded-full flex items-center justify-center mx-auto mb-4">
                    <i data-lucide="shopping-cart" class="text-3xl text-gray-300"></i>
                </div>
                <h5 class="text-lg font-bold text-gray-800">لا توجد مبيعات</h5>
                <p class="text-gray-500 mt-1">ابدأ بإضافة عملية بيع جديدة</p>
            </div>
        }
    </div>
</div>

@section Scripts {
    <script src="~/lib/sweetalert2/sweetalert2.min.js"></script>
    <script src="~/js/sweetalert-sales.js"></script>
    <script src="~/js/salejs/utilities.js"></script>
}


